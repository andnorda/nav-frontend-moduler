import * as React from 'react';
import Alert, { AlertBaseProps } from './alert';

const AlertWarning = React.forwardRef<HTMLDivElement, AlertBaseProps>((props, ref) => {
    return <Alert ref={ref} type="advarsel" {...props} />;
});

export default AlertWarning;
