import * as React from 'react';
import Alert, { AlertBaseProps } from './alert';

const AlertSuccess = React.forwardRef<HTMLDivElement, AlertBaseProps>((props, ref) => {
    return <Alert ref={ref} type="suksess" {...props} />;
});

export default AlertSuccess;
