import * as React from 'react';
import Alert, { AlertBaseProps } from './alert';

const AlertInfo = React.forwardRef<HTMLDivElement, AlertBaseProps>((props, ref) => {
    return <Alert ref={ref} type="info" {...props} />;
});

export default AlertInfo;
