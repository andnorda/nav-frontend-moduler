import * as React from 'react';
import Alert, { AlertBaseProps } from './alert';

const AlertError = React.forwardRef<HTMLDivElement, AlertBaseProps>((props, ref) => {
    return <Alert ref={ref} type="feil" {...props} />;
});

export default AlertError;
