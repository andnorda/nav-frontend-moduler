import * as React from 'react';
import ButtonBase, { ButtonBaseProps } from './index';

const DangerButton = React.forwardRef<HTMLButtonElement, ButtonBaseProps>((props, ref) => {
    return <ButtonBase ref={ref} type="fare" {...props} />;
});
export default DangerButton;
