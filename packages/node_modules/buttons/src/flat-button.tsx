import * as React from 'react';
import ButtonBase, { ButtonBaseProps } from './index';

const FlatButton = React.forwardRef<HTMLButtonElement, ButtonBaseProps>((props, ref) => {
    return <ButtonBase ref={ref} type="flat" {...props} />;
});
export default FlatButton;
