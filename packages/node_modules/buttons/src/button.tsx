import * as React from 'react';
import ButtonBase, { ButtonBaseProps } from './index';

const Button = React.forwardRef<HTMLButtonElement, ButtonBaseProps>((props, ref) => {
    return <ButtonBase ref={ref} type="standard" {...props} />;
});
export default Button;
