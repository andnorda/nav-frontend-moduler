import * as React from 'react';
import ButtonBase, { ButtonBaseProps } from './index';

const MainButton = React.forwardRef<HTMLButtonElement, ButtonBaseProps>((props, ref) => {
    return <ButtonBase ref={ref} type="hoved" {...props} />;
});
export default MainButton;
