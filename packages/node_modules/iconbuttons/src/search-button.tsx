import * as React from 'react';
import Ikon from 'nav-frontend-ikoner-assets';
import ButtonBase, { ButtonBaseProps } from 'buttons';

const SearchButton = React.forwardRef<HTMLButtonElement, ButtonBaseProps>((props, ref) => {
    return (
        <ButtonBase ref={ref} kompakt {...props}>
            <Ikon kind="søk" />
            {props.children || <span>Søk</span>}
        </ButtonBase>
    );
});
export default SearchButton;
