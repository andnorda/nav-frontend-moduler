import * as React from 'react';
import Ikon from 'nav-frontend-ikoner-assets';
import ButtonBase, { ButtonBaseProps } from 'buttons';

const SystemButton = React.forwardRef<HTMLButtonElement, ButtonBaseProps>((props, ref) => {
    return (
        <ButtonBase ref={ref} type="flat" kompakt {...props}>
            <Ikon kind="systemer" />
            {props.children || <span>Systemer</span>}
        </ButtonBase>
    );
});
export default SystemButton;
