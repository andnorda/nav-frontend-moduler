import * as React from 'react';
import Chevron from 'nav-frontend-chevron';
import ButtonBase, { ButtonBaseProps } from 'buttons';

const MenuButton = React.forwardRef<HTMLButtonElement, ButtonBaseProps>((props, ref) => {
    return (
        <ButtonBase ref={ref} kompakt {...props}>
            {props.children || <span>Meny</span>}
            <Chevron type="ned" />
        </ButtonBase>
    );
});
export default MenuButton;
