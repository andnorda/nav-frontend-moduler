import * as React from 'react';
import Ikon from 'nav-frontend-ikoner-assets';
import ButtonBase, { ButtonBaseProps } from 'buttons';

const XButton = React.forwardRef<HTMLButtonElement, ButtonBaseProps>((props, ref) => {
    return (
        <ButtonBase ref={ref} type="flat" kompakt {...props}>
            <Ikon kind="x" />
            {props.children || <span className="sr-only">Lukk</span>}
        </ButtonBase>
    );
});
export default XButton;
